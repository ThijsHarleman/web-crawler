spring:
  application:
    name: web-crawler

  datasource:
    url: jdbc:sqlite:./data/crawler.db
    driver-class-name: org.sqlite.JDBC

  sql:
    init:
      mode: always

server:
  port: 8080

crawler:
  rate-limit:
    minimum-delay: 1s
